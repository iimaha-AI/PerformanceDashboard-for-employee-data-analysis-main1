name: Run Tests 
on: [push] 
jobs: 
 
  build: 
    runs-on: ubuntu-latest 
    steps: 
    - uses: actions/checkout@v3 
    - name: Set up Python 
      with: 
    - name: Install dependencies 
      run: pip install -r requirements.txt 
    - name: Run tests 
    - name: Run tests 
      run: pytest test_employee_events.py 
